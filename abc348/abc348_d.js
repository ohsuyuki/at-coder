function main(input) {
    input = input.split(`\n`);
    const [H, W] = input[0].split(` `).map(Number);
    
}

// const a = main(require("fs").readFileSync("/dev/stdin", "utf8"));
const a = main(``)

console.log(a);
